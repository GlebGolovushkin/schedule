@model IEnumerable<SheaduleASP.TEACHER>

@{
    //Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Преподаватели</title>
</head>
<body>
    <p>
        @Html.ActionLink("Добавить преподавателя", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                <p>ФИО преподавателя</p>
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TEACHER_NAME)
            </td>
            <td>
                @Html.ActionLink("Редактировать", "Edit", new { id=item.TEACHER_CODE }) |
                @Html.ActionLink("Информация", "Details", new { id=item.TEACHER_CODE }) |
                @Html.ActionLink("Удалить", "Delete", new { id=item.TEACHER_CODE })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
