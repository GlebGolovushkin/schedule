@model IEnumerable<SheaduleASP.DISCIPLINE>

@{
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Добавить предмет", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                <p>Название</p>
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DISCIPLINE_NAME)
            </td>
            <td>
                @Html.ActionLink("Редактировать", "Edit", new { id=item.DISCIPLINE_CODE }) |
                @Html.ActionLink("Информация", "Details", new { id=item.DISCIPLINE_CODE }) |
                @Html.ActionLink("Удаление", "Delete", new { id=item.DISCIPLINE_CODE })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
